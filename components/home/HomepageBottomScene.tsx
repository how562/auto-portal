/** Mirrors hero blueprint grid + drift lines at the bottom of the homepage. */
export function HomepageBottomScene() {
  return (
    <div className="homepage-bottom-scene" aria-hidden>
      <div className="homepage-bottom-scene__blueprint" />
      <div className="homepage-bottom-scene__lines" />
    </div>
  );
}
